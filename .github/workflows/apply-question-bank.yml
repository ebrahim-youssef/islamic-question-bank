name: Apply verified question bank
on:
  push:
    branches: [main]
    paths: ['.github/workflows/apply-question-bank.yml']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply payload
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python - <<'PY'
          print('payload placeholder')
          PY
