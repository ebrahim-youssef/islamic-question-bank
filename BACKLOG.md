# ما هو قادم

عمل مُتفق عليه ولم يُبنَ بعد. كل بند هنا وراءه قرار، فهذا الملف هو المرجع قبل
اقتراح أي تغيير في هذه المواضع. يخرج البند من هنا حين يُنفَّذ أو حين يُنقض قراره.

التطبيق المستهلك لهذا البنك هو [`fastabiqu`](https://github.com/ebrahim-youssef/arkan)،
وله ملف `docs/BACKLOG.md` خاص به يحمل الجانب التطبيقي من العمل نفسه.

## مجموعات الإنجاز (achievement sets)

قُرِّر في 2026-08-31. بُني منه شكل البيانات في `data/achievement-sets/` مع بواباته
في `scripts/validate.py`. الباقي مذكور في آخر هذا القسم.

يحتاج التطبيق إلى تعريف **مجموعة إنجاز**: قائمة ثابتة من معرِّفات الأسئلة، تُكمَل
حين يجيب اللاعب عن كل سؤال فيها إجابة صحيحة مرة واحدة على الأقل. القاعدة الحاكمة
في التطبيق أن الإنجاز لا يُسحب بعد منحه أبدًا، ولا يُنقَض حين يكبر البنك. ولهذا
يجب أن تكون قائمة المعرِّفات **ثابتة بعد النشر**، لا قاعدةً تُحسب وقت التشغيل:
مجموعة معرَّفة بأنها «كل أسئلة هذه الفئات» تكبر تلقائيًا عند إضافة سؤال، وهو ما
يكسر القاعدة من جهة المحتوى نفسه.

الشكل المعتمد:

```
data/
  achievement-sets/<set-id>.json
```

وكل ملف يحمل `id` و`name` و`description` و`questionIds`، وأولها
`pillars-foundation.json` وفيه الأسئلة الـ270 الموجودة اليوم.

**عن التسمية.** لفظ `collection` مستعمَل هنا بمعنيين آخرين: حقل `collection` في
مراجع الحديث، وملف `content/collections.json` للتجميعات الموضوعية. فاختير
`achievement-set` تفاديًا للالتباس.

**البوابات في CI:**

- ✅ كل معرِّف في المجموعة موجود فعلًا في `data/questions/` على ذلك الفرع.
- ✅ لا تكرار داخل المجموعة الواحدة.
- ⬜ مجموعة موجودة سلفًا على الفرع الهدف لا تتغير قائمتها. هذه هي البوابة التي
  تحرس ثبات الإنجاز، وتلزم على `release` بذاته، لنفس سبب فحص البيان. لم تُبنَ بعد
  لأنها تحتاج قراءة تاريخ git لا الملفات وحدها، ولم يُحسم بعد أتقارن بالفرع الهدف
  أم بملف قفل مُودَع في المستودع.

**أثر على `data/manifest.json`:** اكتسب قسمًا للمجموعات يحمل المسار وتجزئة
SHA-256 وعدد البايتات وعدد الأسئلة، داخلًا في `bankHash` كما تدخل ملفات الأسئلة.
ولذلك تغيّر `bankHash` دون أن يتغير أي سؤال، وهذا صحيح: المستهلك يقارن ملفًا ملفًا
فلا ينزّل إلا الجديد.

**سياسة محتوى يترتب عليها:** سؤال يدخل مجموعة منشورة لا يُحذف من `release`، وإلا
صارت المجموعة غير قابلة للإكمال. فلا تُنشر مجموعة إلا بعد وصول أسئلتها إلى
`release`.

## شجرة `content/` القديمة

`content/` تحمل 58 ملفًا أوسع وأقدم من `data/`، وهي ليست مرجعية ولا توجد على
`release`، ولا يتحقق منها `scripts/validate.py`. كذلك `schema/collection.schema.json`
لا يستعمله أي فاحص. يبقى القرار قائمًا: إما التوفيق بينها وبين `data/` أو حذفها،
ولا ينبغي أن تبقى على حالها هذه طويلًا.
