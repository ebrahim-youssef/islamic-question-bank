# ما هو قادم

عمل مُتفق عليه ولم يُبنَ بعد. كل بند هنا وراءه قرار، فهذا الملف هو المرجع قبل
اقتراح أي تغيير في هذه المواضع. يخرج البند من هنا حين يُنفَّذ أو حين يُنقض قراره.

التطبيق المستهلك لهذا البنك هو [`fastabiqu`](https://github.com/ebrahim-youssef/fastabiqu)،
وله ملف `docs/BACKLOG.md` خاص به يحمل الجانب التطبيقي من العمل نفسه.

## الإنجازات: ما الذي يلزم البنكَ منها

قُرِّر في 2026-08-31 أن يحمل البنك «مجموعات إنجاز»: قوائم ثابتة من معرِّفات
الأسئلة في `data/achievement-sets/`. نُقض القرار في 2026-09-03 وأُزيلت المجموعات
وبواباتها وملف مخططها من هذا المستودع.

سبب النقض أن القائمة الثابتة كانت تحل مشكلة لم تكن قائمة. القاعدة التي أراد
التطبيق حراستها هي أن الإنجاز لا يُسحب بعد منحه، وقد صار يحرسها بأن يكتب الإنجاز
مرة واحدة عند نيله ولا يعيد حسابه بعدها، فلم يبقَ للقائمة الثابتة عمل. صار
الإنجاز عتبةً على عدد الأسئلة المختلفة التي أجاب عنها اللاعب إجابة صحيحة داخل
الفئة، تُحسب كلها في التطبيق من سجل إجاباته.

فليس في البنك اليوم مفهوم إنجاز البتة، ولا يُنتظر أن يعود. الذي يلزم البنكَ منها
اثنان، وكلاهما قائم:

- **ثبات معرِّفات الأسئلة.** المعرِّف هو ما يميز سؤالًا أجيب عنه من سؤال لم
  يُجَب، فإعادة تسميته تُنقص عدّاد اللاعب. `scripts/validate.py` يمنع التكرار،
  ولا يوجد ما يمنع إعادة التسمية، وهو ما يستحق بوابةً تقارن بالفرع الهدف.
- **ألا تنزل فئة على `release` تحت عتبة التطبيق.** العتبة اليوم خمسون سؤالًا في
  الفئة الواحدة. فئة تصل إلى `release` بأقل منها تجعل إنجازها غير قابل للنيل.
  الفئات الخمس تحمل اليوم 51 و56 و55 و53 و55 سؤالًا، فكلها فوق العتبة.

**سياسة محتوى يترتب عليها:** لم يعد حذف سؤال من `release` يُفسد إنجازًا بعينه،
لأن الإنجاز عتبة لا قائمة. لكنه ينقص عدّاد الفئة، فلا يُحذف سؤال إذا كان حذفه
ينزل بالفئة تحت العتبة.

## شجرة `content/` القديمة

`content/` تحمل 58 ملفًا أوسع وأقدم من `data/`، وهي ليست مرجعية ولا توجد على
`release`، ولا يتحقق منها `scripts/validate.py`. كذلك `schema/collection.schema.json`
لا يستعمله أي فاحص. يبقى القرار قائمًا: إما التوفيق بينها وبين `data/` أو حذفها،
ولا ينبغي أن تبقى على حالها هذه طويلًا.
