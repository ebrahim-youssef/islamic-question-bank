{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "islamic-question-bank/schema/question.schema.json",
  "title": "سؤال V2",
  "type": "object",
  "required": [
    "id",
    "categoryId",
    "ageBand",
    "tier",
    "text",
    "choices",
    "correctChoiceId",
    "explanation",
    "references",
    "tags",
    "verification"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "categoryId": {
      "type": "string",
      "minLength": 1
    },
    "ageBand": {
      "enum": ["kids", "general", "scholar"]
    },
    "tier": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5
    },
    "text": {
      "type": "string",
      "minLength": 1
    },
    "choices": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "type": "object",
        "required": ["id", "text"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-d]$"
          },
          "text": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "correctChoiceId": {
      "type": "string",
      "pattern": "^[a-d]$"
    },
    "explanation": {
      "type": "string",
      "minLength": 1
    },
    "references": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "enum": ["quran", "hadith", "book", "other"]
          }
        },
        "required": ["type"],
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": { "const": "quran" },
              "surah": { "type": "integer", "minimum": 1, "maximum": 114 },
              "ayah": { "type": "integer", "minimum": 1 }
            },
            "required": ["type", "surah", "ayah"],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": { "const": "hadith" },
              "collection": { "type": "string", "minLength": 1 },
              "number": { "type": "string", "minLength": 1 }
            },
            "required": ["type", "collection", "number"],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": { "const": "book" },
              "title": { "type": "string", "minLength": 1 },
              "locator": { "type": "string", "minLength": 1 }
            },
            "required": ["type", "title"],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": { "const": "other" },
              "label": { "type": "string", "minLength": 1 }
            },
            "required": ["type", "label"],
            "additionalProperties": false
          }
        ]
      }
    },
    "tags": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "verification": {
      "type": "object",
      "required": ["status", "verifiedAt"],
      "additionalProperties": false,
      "properties": {
        "status": {
          "enum": ["pending", "verified", "needs_review"]
        },
        "verifiedAt": {
          "type": ["string", "null"],
          "format": "date-time"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "status": { "const": "verified" } },
            "required": ["status"]
          },
          "then": {
            "properties": {
              "verifiedAt": { "type": "string", "format": "date-time" }
            }
          },
          "else": {
            "properties": {
              "verifiedAt": { "type": "null" }
            }
          }
        }
      ]
    }
  }
}
